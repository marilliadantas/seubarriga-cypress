name: E2E Tests

on: 
  push:
    branches:
      - main
      - development
  pull_request:
    branches:
      - main

jobs:
  ui-tests:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        browser: [chrome, edge, firefox]  # Definindo a matriz de navegadores
    steps:
      - name: 📁 Copying the Project
        uses: actions/checkout@v4

      - name: 🧪 Running Tests on ${{ matrix.browser }}
        uses: cypress-io/github-action@v6
        with:
          browser: ${{ matrix.browser }}  # Usando o valor da matriz para o navegador
        env:
          CYPRESS_EMAIL_VALID: ${{ secrets.EMAIL_VALID }}
          CYPRESS_PASSWORD_VALID: ${{ secrets.PASSWORD_VALID }}
          CYPRESS_EMAIL_INVALID: ${{ secrets.EMAIL_INVALID }}
          CYPRESS_PASSWORD_INVALID: ${{ secrets.PASSWORD_INVALID }}
